# ---------------------------------------------------------------------------
# Game assets MUST NEVER be committed to this repository.
# This project documents formats; it does not redistribute content.
# ---------------------------------------------------------------------------
extract/
game/
*.bin
*.exe
*.dll
*.wav
*.pvr
*.pvp
*.pal
*.png
*.bmp
*.tga
*.gltf
*.glb

# Explicit allowances (generated metadata and docs, no asset content)
!manifest.csv
!inventory.csv
!extract/README.md

# Web player
# The client's source is committed; nothing it consumes or produces is.
# `extract/player/` is game-derived data and is already covered by `extract/`
# above -- it is named again here because it is the one directory a reader is
# most likely to be tempted to commit.
extract/player/
web/node_modules/
web/dist/
web/.vite/
web/test-results/
web/playwright-report/
# Local-only visual diff output. tests/README.md sets the policy: assertions
# are against hashes and derived metrics, never against embedded asset
# content -- and a screenshot of a stage IS derived game art.
web/test/visual/**/*-actual.png
web/test/visual/**/*-diff.png
web/test/visual/screenshots/

# Ghidra
# Project databases and analysis output are local; the scripts that produce
# them are committed, because reproducibility is the point.
*.gpr
*.rep/
ghidra/project/
ghidra/out/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Editors / OS
.DS_Store
.vscode/
.idea/
