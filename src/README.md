# src

Readable C reference implementations of the RE'd subsystems. Phase 7.

Documentation-grade, not byte-matching: the goal is that a reader can understand
the format from the code, and that it compiles and runs independently of the
original binary.

Planned: `lz.c` (Phase 2), `container.c`, `nl1.c`, `texbank.c`, `material.c`.
